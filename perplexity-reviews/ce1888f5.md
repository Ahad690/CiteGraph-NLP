---
commit: ce1888f576c4b366119e476cac53bb5fe76c3070
short_sha: ce1888f5
author: Ahad Imran
date: 2026-05-23
message: "Fix frontend production issues from full codebase audit\n\n- Use env var VITE_API_BASE instead of hardcoded localhost for API URL\n- Stop silently returning mock data on network errors (only demo_run_001 uses mock)\n- Fix missing React key on Fragment in CitationPathsPage\n- Fix invalid HTML nesting (div onClick inside button) in PopulationPage\n- Add clipboard .catch() error handling in PaperDetailDrawer and RankingsPage\n- Fix onWheel passive listener issue in CitationGraph (use addEventListener)\n- Fix graph centering: use clientWidth/clientHeight, useLayoutEffect, SVG display block\n- Memoize foundationalSet in MetadataPage\n- Fix broken /dashboard/demo route in FinalCTA\n- Add unique aria-labels on Footer social links\n- Fix twitter:site meta from @Lovable to @CiteGraphNLP\n\nCo-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
model: gpt-5-5-thinking
provider: chatgpt
thinking_effort: extended
reviewed_at: 2026-05-22T22:18:00.735Z
---

# Code Review: ce1888f5

> Fix frontend production issues from full codebase audit

## Summary

This commit (`frontend`) addresses a series of production issues uncovered during a full codebase audit. The changes include switching the API base URL to use `VITE_API_BASE` instead of hardcoded localhost (`api.ts:L7`), limiting mock fallback behavior, fixing React key issues (`CitationPathsPage.tsx:L70-L94`), correcting invalid HTML nesting (`PopulationPage.tsx:L87-L96`), adding robust clipboard error handling (`PaperDetailDrawer.tsx:L19-L21` and `RankingsPage.tsx:L119-L121`), fixing passive event listener issues and improving graph centering (`CitationGraph.tsx:L34-L188`), memoizing a derived set (`MetadataPage.tsx:L17`), repairing broken demo routes (`FinalCTA.tsx:L34-L38`), and adding accessibility improvements (`Footer.tsx:L18-L28`) including a corrected Twitter meta tag (`__root.tsx:L79`).

## Issues & Suggestions

- **API Base & Mock Handling** (`api.ts:L7-L50`):  
  - Positive: Using `import.meta.env.VITE_API_BASE` is correct for production builds.  
  - Suggestion: Consider validating that `VITE_API_BASE` is non-empty and HTTPS for production. A malformed or missing env could silently fail requests.

- **React Keys** (`CitationPathsPage.tsx:L70-L94`):  
  - Positive: Moving `key={pid}` to `Fragment` eliminates duplicate key warnings.  
  - Suggestion: Ensure `pid` is unique across nested loops. Otherwise, collisions may persist.

- **HTML Semantics** (`PopulationPage.tsx:L87-L96`):  
  - Positive: Changing `div` inside `button` to `span` with `role="button"` improves accessibility and validity.  
  - Suggestion: Consider adding keyboard `onKeyDown` handling for `Enter`/`Space` for full accessibility.

- **Clipboard Error Handling** (`PaperDetailDrawer.tsx:L19-L21`, `RankingsPage.tsx:L119-L121`):  
  - Positive: Adding `.catch()` prevents unhandled promise rejections.  
  - Suggestion: The RankingsPage handler silently swallows errors (`catch(() => {})`). Consider logging or notifying the user to improve UX.

- **Passive Wheel Event & Graph Centering** (`CitationGraph.tsx:L34-L188`):  
  - Positive: `useLayoutEffect` ensures correct client size read before paint, `addEventListener` with `{ passive: false }` solves `preventDefault` issues.  
  - Suggestion: Ensure `ResizeObserver` plus `useLayoutEffect` doesn’t cause double render or layout thrashing under frequent resize. Consider throttling updates if performance issues arise.

- **Memoization** (`MetadataPage.tsx:L17`):  
  - Positive: `useMemo` prevents unnecessary recalculation of `foundationalSet`.  
  - Suggestion: Confirm `run.ranked_foundational_papers` identity doesn’t change unnecessarily, or wrap in stable reference.

- **Demo Route Fix** (`FinalCTA.tsx:L34-L38`):  
  - Positive: `/dashboard/$runId` with `params={{ runId: "demo_run_001" }}` aligns with dynamic routing expectations.  
  - Suggestion: Validate that all links using `$runId` in other components follow the same pattern.

- **Footer Accessibility & SEO** (`Footer.tsx:L18-L28`, `__root.tsx:L79`):  
  - Positive: Unique `aria-label` for each social link and correct Twitter site meta improves accessibility and SEO.  
  - Suggestion: Verify the `href="#"` placeholder is replaced with real links in production.

## Security & Performance

- **Security**: No new major vulnerabilities introduced; clipboard handling `.catch()` improves runtime safety. Use of env var for API URL is appropriate but should validate origin to avoid accidental local requests in production (`api.ts:L7`).  
- **Performance**: `useLayoutEffect` and `ResizeObserver` in `CitationGraph.tsx:L34-L188` could trigger extra layout passes on frequent resizes.  
- **Scalability**: Memoization and event listener improvements scale well. Dynamic routing and demo data fixes ensure production behavior remains predictable under multiple runs.

## Overall Score: 9/10

- The commit resolves multiple production issues, improves accessibility, and fixes React warnings.  
- Minor improvements could be made in clipboard error handling and performance validation under heavy resize events.

## Recommendations

1. Validate `VITE_API_BASE` and ensure it is HTTPS in production (`api.ts:L7`).  
2. Enhance RankingsPage clipboard `.catch()` to notify/log failures for UX feedback (`RankingsPage.tsx:L119-L121`).  
3. Confirm `pid` uniqueness in `CitationPathsPage` across all loops to avoid React key collisions (`CitationPathsPage.tsx:L70-L94`).  
4. Add keyboard accessibility for interactive `span[role="button"]` in `PopulationPage.tsx:L87-L96`.  
5. Monitor layout performance with `ResizeObserver` and `useLayoutEffect` on large graphs (`CitationGraph.tsx:L34-L188`).  
6. Replace `href="#"` placeholders in `Footer.tsx:L18-L28` with production URLs to prevent focus and navigation issues.  

This commit effectively addresses the audit findings and stabilizes frontend production behavior.