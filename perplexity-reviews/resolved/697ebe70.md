---
commit: 697ebe70bb4468c3a94225058c657b185960975d
short_sha: 697ebe70
author: Ahad Imran
date: 2026-05-20
message: "fix: address review feedback on db lock coverage, taskmanager cleanup, case-insensitivity, and robust URL parsing"
model: gpt-5-5-thinking
provider: chatgpt
thinking_effort: extended
reviewed_at: 2026-05-20T06:44:16.830Z
---

# Code Review: 697ebe70

> fix: address review feedback on db lock coverage, taskmanager cleanup, case-insensitivity, and robust URL parsing

## Verdict
**NEEDS WORK** — The commit improves DOI/PMID/PMCID canonicalization, SQLite lock coverage, and TaskManager shutdown cleanup, but gaps remain in edge-case handling, task registration completeness, and URL normalization that could cause runtime failures or incorrect metadata.

## Implementation vs Intent Gap
- TaskManager now preserves unfinished tasks instead of clearing them (tasks.py L39-40), which partially changes lifecycle behavior but may leave tasks lingering indefinitely, violating full “cleanup” intent.  
- URL parsing in `EuropePMCProvider` (providers/europe_pmc.py L33-61) now checks path, query, fragment, and parameters, but does not canonicalize extracted IDs before returning them, unlike the InputNormalizer and IdCanonicalizer updates (input/normalizer.py L5-13, utils/ids.py L18-22).  
- SQLite `_execute` helper (storage/sqlite.py L50-57) adds commit flag but **does not include retry logic**—previously `_execute_with_retry` existed but is now removed; atomicity under transient connection failure is not addressed.

## Bugs & Failure Modes
| File:Line | Severity | Finding | Evidence |
|-----------|----------|---------|----------|
| src/citegraph/utils/tasks.py:39 | ⚠️ High | TaskManager may retain cancelled tasks indefinitely | `_tasks` is filtered for unfinished tasks only; long-running or stuck tasks may persist beyond shutdown |
| src/citegraph/providers/europe_pmc.py:33-61 | ⚠️ Medium | Extracted DOI/PMID/PMCID may not be canonicalized | `normalize_doi`/`normalize_pmid` exist but not applied in provider; inconsistent ID formats could propagate |
| src/citegraph/storage/sqlite.py:50-57 | ⚠️ Medium | No retry for transient SQLite failures | `_execute` is a simple wrapper; original `_execute_with_retry` removed, leaving potential for failed writes under SQLite locking contention |
| src/citegraph/input/normalizer.py:5-13 | ⚠️ Low | Helper functions exist but not integrated with EuropePMCProvider | Provider bypasses canonicalizer; case-insensitive URLs may not normalize correctly |

## Missing Changes
- EuropePMCProvider should apply `InputNormalizer` or `IdCanonicalizer` to all extracted IDs before returning.  
- Consider adding **retry logic** for `_execute` in SQLiteStore to handle SQLite “database is locked” errors.  
- Background tasks created outside `start_run` or similar routes are not registered with `task_manager`.  
- URL validation in `PaperQuery` (models/paper.py) should integrate fully with provider parsing for consistency.

## Security & Data Integrity
- Non-canonical DOI/PMID/PMCID could result in **duplicate or misidentified runs**, corrupting analytics or indexing.  
- SQLite writes without retry may **fail silently under high concurrency**, leading to lost run data or inconsistent status.  
- Unfinished background tasks after shutdown could **mutate database or trigger unexpected side effects**, potentially leading to corrupted results.

## Score: 7/10
Evidence: TaskManager partially revised (tasks.py L39-40), SQLite locking added but lacks retry (sqlite.py L50-57), canonicalization exists but EuropePMCProvider ignores it (input/normalizer.py L5-13 vs providers/europe_pmc.py L33-61). Improvements are clear, but real-world concurrency and data consistency issues remain.